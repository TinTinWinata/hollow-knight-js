import { Object } from "../parent/object";

export const ground = new Ground();

class Ground extends Object {
  constructor(x, y, w, h, sprite) {
    super(x, y, w, h);
  }

  render(){
      
  }
}
